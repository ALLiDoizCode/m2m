# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# STATUS (#600, 2026-07-30): NOT DEPLOYED. The devnet store leg shipped
# WITHOUT this overlay: the apex terminates g.toon.ario / g.toon.store at
# the store app over this box's own PUBLIC TLS front
# (https://proxy.store.devnet.toonprotocol.dev/store — nginx's `location
# /store` in ./nginx/conf.d/node.conf; see
# infra/linode-node/connector-rust.toml's header). Two findings closed
# that door for this overlay:
#   * the PAID forwarded path is structurally unwired (#620) — a peer
#     route is neither priced nor charged at the client edge, a peer-wire
#     arrival is never charged the terminating route's price, and ADR
#     0024's peer claims cannot be configured from any connector.toml, so
#     deploying this today would stand up a free-write path beside a paid
#     one; and
#   * the peer wire cannot carry the public inter-node link devnet
#     topology calls for (#623) — raw-TCP framing nginx cannot
#     TLS-terminate, no TLS of its own (ADR 0003), SocketAddr-only
#     addressing. The VPC these files' comments were written for was
#     never provisioned, and #600 settled that no private segment is
#     wanted: node↔node links are public, like the TS fleet's BTP-over-wss.
# When #620 + #623 land, STORE_PEER_WIRE_BIND's whole containment story
# below should be revisited against the transport they choose.
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# ═══════════════════════════════════════════════════════════════════════════
# TOON devnet — Rust connector, store side (issue #490, ADR 0013's parallel
# fleet)
# ═══════════════════════════════════════════════════════════════════════════
# Mirrors infra/linode-node/connector-rust.toml's own header for the prefix
# reasoning: `g.toon` is the protocol's own namespace, served by this fleet
# since it became the official implementation (2026-07-30; the parallel
# `g.rust` prefix is historical). Runs ALONGSIDE
# ./connector.yaml's TypeScript node on this box — see
# docker-compose.store.rust.yml, an ADDITIVE overlay that never edits
# docker-compose.store.yml.
#
# Terminates `g.toon.store` at the SAME running store (Arweave DVM) app this
# box's TypeScript connector already fronts (store:3300) — payment-
# oblivious per ADR 0013, and NOT modified by this file. Accepts the
# node-side Rust connector's peer-wire connection (peer_wire_addr below); it
# never dials out itself, so there is no [[peers]] section here.
# ═══════════════════════════════════════════════════════════════════════════

# Distinct from the TypeScript node's 3000/8080/8081, matching the node-side
# connector-rust.toml's own port choice.
client_edge_addr = "0.0.0.0:4000"

# The accepting side of the peer wire — the node-side apex dials this. Port
# matches that apex's [[peers]] entry for id = "store".
#
# `0.0.0.0` here is every interface *inside this container's own network
# namespace*, which is not the same thing as every interface on the box. The
# question of which host interface the port is reachable on is settled one
# layer up, by docker-compose.store.rust.yml's `${STORE_PEER_WIRE_BIND}:4001`
# publish — see that file's header for why the answer must be a VPC address
# and never the public one (#492). Binding narrower than `0.0.0.0` here would
# not help: the container cannot see the host's VPC interface, so the only
# address it could bind is its own, which is what this already is.
peer_wire_addr = "0.0.0.0:4001"

# ── Durable claim state (issue #605) ──────────────────────────────────────
# Where this node writes the claim journals whose replay is what makes a
# replay watermark survive a restart. Without it, `ClientClaimGate` (and
# `ClaimBook`) keep every channel's watermark in process memory only, so a
# `docker compose up -d`, a crash or an OOM kill resets every channel to "no
# claim ever seen" -- and a gate with no watermark accepts any nonce, which
# makes every claim a client already spent free service again.
#
# This MUST be a mounted volume, not a path inside the container's writable
# layer: a path that dies with the container is a watermark that dies with
# the container, which is the same defect one indirection down. See
# ./docker-compose.store.rust.yml's `connector_rust_state` volume, and note
# the container runs as uid 10001 -- a named volume inherits that owner
# from the image's own /app/state, a bind mount of a host directory does
# not (`chown 10001:10001` it first).
#
# Config load refuses `[[client_channels]]` without this, and startup
# refuses to boot at all if the directory cannot be written -- loudly, with
# the path, rather than at the first claim hours later.
state_dir = "/app/state"

[signer]
# Real key material, generated per-box, never committed — see
# infra/linode-node/connector-rust.toml's own [signer] comment.
key_file = "/app/data/signer.key"

[[routes]]
prefix = "g.toon.store"
# `/store`, not the bare origin — the same defect the node-side
# connector-rust.toml's `g.toon.relay` route carried, for the same reason:
# `HttpAppClient::deliver` POSTs here, and the store app serves only
# `POST /store` and `GET /health`.
#
# An envelope's own `target` cannot override this path (issue #596, ADR
# 0025): it is resolved strictly beneath `/store`, never in place of it, so
# a sender cannot address `/health` (or anywhere else) through this route at
# this route's price by naming it as the target.
#
# UNVERIFIED against a running box. #492 deployed the apex only — the store
# box did not exist at the time — so the relay side of this pair was proven
# live (404 → 400 once the path was added) and this side was corrected by
# reading the store app's routes, not by driving a packet at it. Confirm it
# when the store box comes back.
handler_url = "http://store:3300/store"
# Priced at parity with the TypeScript fleet's own g.toon.ario/
# g.toon.relay.ario routes to this same store (infra/linode-node/
# connector.yaml -- `price: '1000'`): see infra/linode-node/
# connector-rust.toml's own comment on `price` (issue #557) for why the
# same integer is the same real-world charge -- both fleets settle in
# base units of 6-decimal USDC (ADR 0010). This route was still a free
# gateway with #552's x402 greeting landed but nothing here priced above
# zero to trigger it (issue #557).
price = 1000

# ── Operator surface (optional) ─────────────────────────────────────────
# See infra/linode-node/connector-rust.toml's own note — omitted for the
# same reason.

# ── Settlement (issue #542) — DELIBERATELY NOT CONFIGURED ───────────────
# See infra/linode-node/connector-rust.toml's own note for the full
# reasoning. In short: an ABSENT `[settlement]` section is the supported way
# to run without settlement (channel operations answer
# `NoSettlementBackend`, the node starts and serves), while a section that
# is PRESENT but names a registry with no `TokenNetwork` registered for the
# configured token is a hard startup failure by design (ADR 0009,
# fail-closed) — `EvmSettlementBackend::connect` resolves `token_address`
# through `contract_address`'s `TokenNetworkRegistry.getTokenNetwork`, gets
# the zero address back, and the process exits 1. So the zero address is
# not a harmless placeholder here.
#
#
# One further consequence, since issue #556: an absent `[settlement]`
# section is also the absence of a client-edge channel *source*. With no
# chain to ask, the only channels this node can accept a claim on are the
# ones a `[[client_channels]]` section declares -- so an unaffiliated buyer
# who opens a channel on chain cannot pay here until #577 fills the section
# below in. The mechanism is landed; this box is not yet pointed at a
# chain.
# `EvmSettlementBackend` drives `packages/contracts/src/TokenNetwork.sol`,
# reached through that registry (issue #566, issue #576) --
# `contracts/SettlementChannel.sol`, this crate's own throwaway,
# signature-unverified channel contract, is gone.
#
# The template below is the shape to fill in once this box is meant to
# settle for real: `contract_address` names the `TokenNetworkRegistry`, not
# a channel contract directly. Same real USDC (`./connector.yaml`'s
# `tokenAddress`), same known-good RPC, same key file reused for settlement
# signing. It is uncommented and booted against a freshly deployed registry
# and `TokenNetwork` on a local anvil (the explicit test-only escape hatch)
# by `crates/connector-bin/tests/devnet_configs_load.rs`, so it stays a
# template that actually works. #577 tracks actually shipping this section
# filled in against the live Base Sepolia deployment.
#
# --- BEGIN settlement template (uncomment when a contract is deployed) ---
# [settlement]
# chain = "evm"
# rpc_url = "https://base-sepolia-rpc.publicnode.com"
# # REPLACE the zero address with the deployed `TokenNetworkRegistry` address.
# contract_address = "0x0000000000000000000000000000000000000000"
# token_address = "0x49beE1Bca5d15Fb0963117923403F9498119a9Ce"
# # The scale the deployed token itself reports. Nothing scales by it --
# # claims are already in the token's base units -- but startup reads the
# # token's own `decimals()` and refuses to boot if the two disagree (issue
# # #564), so a stale value here is a refusal to start, not a silent no-op.
# decimals = 6
#
# [settlement.key]
# key_file = "/app/data/signer.key"
# --- END settlement template ---
