{
  "name": "@toon-protocol/gate",
  "version": "0.1.0",
  "private": true,
  "description": "Correctness and no-regression guards for connector's gate, checked against the frozen .sandcastle/gate-baseline.json",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "gate-check": "./dist/cli.js"
  },
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "jest",
    "test:unit": "jest",
    "test:coverage": "jest --coverage",
    "test:watch": "jest --watch",
    "lint": "eslint src --ext .ts"
  },
  "engines": {
    "node": ">=22.11.0"
  },
  "devDependencies": {
    "@types/jest": "^29.5.12",
    "@types/node": "^20.11.19",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.2"
  }
}
