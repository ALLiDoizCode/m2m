diff --git a/node_modules/@anyone-protocol/anyone-client/out/postinstall.js b/node_modules/@anyone-protocol/anyone-client/out/postinstall.js
index 1111111..2222222 100755
--- a/node_modules/@anyone-protocol/anyone-client/out/postinstall.js
+++ b/node_modules/@anyone-protocol/anyone-client/out/postinstall.js
@@ -110,7 +110,16 @@ const makeExecutable = (file) => {
 (() => __awaiter(void 0, void 0, void 0, function* () {
-    const resp = yield axios_1.default.get(releaseUrl);
+    // patch-package (connector #156): authenticate the GitHub release-tag
+    // lookup so CI runners are subject to the 5000 req/hr authenticated rate
+    // limit instead of the 60 req/hr unauthenticated per-IP limit that
+    // routinely 403s macOS runners. Reads GITHUB_TOKEN/GH_TOKEN if present;
+    // falls back to an unauthenticated request when no token is set.
+    const _ghToken = process_1.default.env.GITHUB_TOKEN || process_1.default.env.GH_TOKEN;
+    const _reqCfg = _ghToken
+        ? { headers: { Authorization: `Bearer ${_ghToken}`, 'X-GitHub-Api-Version': '2022-11-28' } }
+        : {};
+    const resp = yield axios_1.default.get(releaseUrl, _reqCfg);
     const osPlatform = os_1.default.platform();
     if (!platformMap.hasOwnProperty(osPlatform)) {
         console.error(`Platform ${osPlatform} is not supported`);
         process_1.default.exit(1);
     }
diff --git a/node_modules/@anyone-protocol/anyone-client/src/postinstall.ts b/node_modules/@anyone-protocol/anyone-client/src/postinstall.ts
index 3333333..4444444 100644
--- a/node_modules/@anyone-protocol/anyone-client/src/postinstall.ts
+++ b/node_modules/@anyone-protocol/anyone-client/src/postinstall.ts
@@ -115,7 +115,19 @@

 (async () => {

-  const resp: AxiosResponse<Release> = await axios.get<Release>(releaseUrl);
+  // patch-package (connector #156): authenticate the GitHub release-tag
+  // lookup so CI runners are subject to the 5000 req/hr authenticated rate
+  // limit instead of the 60 req/hr unauthenticated per-IP limit that
+  // routinely 403s macOS runners. Reads GITHUB_TOKEN/GH_TOKEN if present;
+  // falls back to an unauthenticated request when no token is set.
+  // NOTE: this must live in src/ (not just out/) because the package's
+  // postinstall runs `npm run build` (tsc) which recompiles src -> out and
+  // would otherwise discard an out/-only patch.
+  const _ghToken = process.env.GITHUB_TOKEN || process.env.GH_TOKEN;
+  const _reqCfg = _ghToken
+    ? { headers: { Authorization: `Bearer ${_ghToken}`, 'X-GitHub-Api-Version': '2022-11-28' } }
+    : {};
+  const resp: AxiosResponse<Release> = await axios.get<Release>(releaseUrl, _reqCfg);

   const osPlatform = os.platform();
   if (!platformMap.hasOwnProperty(osPlatform)) {
diff --git a/node_modules/@anyone-protocol/anyone-client/src/process.ts b/node_modules/@anyone-protocol/anyone-client/src/process.ts
index 5555555..6666666 100644
--- a/node_modules/@anyone-protocol/anyone-client/src/process.ts
+++ b/node_modules/@anyone-protocol/anyone-client/src/process.ts
@@ -168,7 +168,11 @@
    */
   public static async isAnonProcessRunning(): Promise<boolean> {
     try {
-      const { stdout } = await execAsync('ps aux | grep anon | grep -v grep');
+      // patch-package (connector #178): pgrep with user-ID filter avoids
+      // false positives from Docker container anon processes (root-owned on
+      // Linux CI), which would otherwise cause AnonRunningError for all
+      // managed-client tests while the testnet containers are running.
+      const { stdout } = await execAsync('pgrep -x -u "$(id -u)" anon');
       return stdout.trim().length > 0;
     } catch (error) {
       return false;
