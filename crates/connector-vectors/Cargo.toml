[package]
name = "connector-vectors"
version = "0.1.0"
edition.workspace = true
license.workspace = true

[dependencies]
connector-domain = { path = "../connector-domain" }
connector-signer = { path = "../connector-signer" }
hex = "0.4"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
