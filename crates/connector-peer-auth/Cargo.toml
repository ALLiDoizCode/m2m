[package]
name = "connector-peer-auth"
version = "0.1.0"
edition.workspace = true
license.workspace = true

[dependencies]
# The configured side of the decision: `[[peers]]` and `[[peer_channels]]`,
# which is the whole of what `decide_role` may consult (ADR 0060 deleted
# the shared secret that used to sit beside them). No transport, no
# runtime, no clock -- see the guard in `lib.rs`.
connector-config = { path = "../connector-config" }
