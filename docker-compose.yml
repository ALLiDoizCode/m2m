# Local Blockchain Development Infrastructure
# Docker Compose with profiles for selective chain startup.
#
# Usage:
#   make anvil-up      Start Anvil + Faucet (EVM)
#   make anvil-down    Stop EVM services
#   make anvil-logs    Follow EVM logs
#   make solana-up     Start Solana test validator
#   make solana-down   Stop Solana services
#   make solana-logs   Follow Solana logs
#   make mina-up       Start Mina lightnet
#   make mina-down     Stop Mina services
#   make mina-logs     Follow Mina logs
#   make infra-up      Start all chains (EVM + Solana + Mina)
#   make infra-down    Stop all chains (EVM + Solana + Mina; preserves volumes)

# ── Shared json-file log rotation for the long-running dev-chain services ──
# Incident 2026-06-26: on the self-hosted devnet box the solana-validator wrote
# a 121 MB UNROTATED Docker json log (it prints a status line ~30x/sec) on top
# of an oversized rocksdb ledger, which helped fill the box disk to 100%. A full
# disk silently FREEZES Solana block production (slot stops advancing) while
# /health still returns "ok", so faucet/settlement writes hang then 500
# ("Transaction was not confirmed in 30s") with no obvious cause. anvil, faucet
# and mina-lightnet are similarly chatty. Cap every such service's container log
# at 30 MB total (10 MB x 3 rotated files) so logs can never fill /var/lib/docker.
# (The solana ledger itself is bounded separately by --limit-ledger-size in
# infra/solana/entrypoint.sh — the two together are the durable disk-full fix.)
x-logging: &default-logging
  driver: json-file
  options:
    max-size: '10m'
    max-file: '3'

services:
  # ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  # Anvil - Local Ethereum Node with Auto-Deployed Contracts
  # ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  anvil:
    image: ghcr.io/foundry-rs/foundry:latest
    # Run as root: the foundry image's default non-root user cannot write
    # forge's out/cache/broadcast dirs into the bind-mounted ./packages/contracts
    # when the host dir is owned by a different UID (e.g. the CI runner) —
    # `forge script` then fails with `"/contracts/out": Permission denied`. See #104.
    user: root
    profiles:
      - evm
      - app # issue #221 — reused by the app profile
    entrypoint: []
    command:
      - sh
      - -c
      - |
        echo "Starting Anvil..."
        anvil --host 0.0.0.0 --port 8545 --chain-id 31337 --accounts 10 --balance 10000 &
        ANVIL_PID=$$!

        echo "Waiting for Anvil to be ready..."
        until cast client --rpc-url http://localhost:8545 2>/dev/null | grep -q 'anvil'; do
          sleep 1
        done

        echo "Deploying contracts..."
        cd /contracts

        # DeployLocal.s.sol imports forge-std + openzeppelin-contracts from
        # lib/ (git submodules). In CI the checkout fetches them via
        # `submodules: recursive`; for local dev / git worktrees where the
        # submodules may not be initialized, fall back to `forge install` so the
        # deploy is self-healing and the stack never comes up without the USDC
        # token + TokenNetworkRegistry. See issue #104.
        if [ ! -f lib/forge-std/src/Script.sol ] || [ ! -f lib/openzeppelin-contracts/contracts/access/Ownable.sol ]; then
          echo "Foundry libs missing — running 'forge install'..."
          forge install foundry-rs/forge-std OpenZeppelin/openzeppelin-contracts --no-git || forge install
        fi

        # Deploy must succeed: the standalone-settlement-e2e harness depends on
        # the USDC token + TokenNetworkRegistry existing at deterministic
        # addresses. A silent failure here (the old `|| echo non-fatal`) is what
        # left the faucet reporting tokenReady:true with no token on-chain (#104).
        forge script script/DeployLocal.s.sol:DeployLocalScript --rpc-url http://localhost:8545 --broadcast --skip-simulation

        echo "Anvil ready with contracts deployed!"
        wait $$ANVIL_PID
    volumes:
      - ./packages/contracts:/contracts
    ports:
      - '8545:8545'
    restart: unless-stopped
    logging: *default-logging
    healthcheck:
      test:
        [
          'CMD-SHELL',
          "cast client --rpc-url http://localhost:8545 2>&1 | grep -q 'anvil' || exit 1",
        ]
      interval: 10s
      timeout: 5s
      retries: 3
      start_period: 15s

  # ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  # Token Faucet - Provides ETH and USDC tokens for testing
  # ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  faucet:
    # Build context is the repo ROOT (not packages/faucet): the image builds the
    # mina-zkapp ESM bundle for the Mina USDC admin-mint path, so the Dockerfile
    # COPYs packages/mina-zkapp + packages/faucet. See packages/faucet/Dockerfile.
    build:
      context: .
      dockerfile: packages/faucet/Dockerfile
    # Runs under BOTH evm and solana so a single host can drip every chain.
    # The EVM init polls for the anvil token deploy; the Solana route NO-OPs
    # with a clear 503 when its env isn't set — so an evm-only `up` still works.
    profiles:
      - evm
      - solana
      - app # issue #221 — reused by the app profile
    environment:
      PORT: 3500
      RPC_URL: http://anvil:8545

      # Account 1 for ETH (has 10k ETH from Anvil)
      ETH_PRIVATE_KEY: '0x59c6995e998f97a5a0044966f0945389dc9e86dae88c7a8412f4603b6b78690d'

      # Account 0 (deployer) for USDC tokens
      TOKEN_PRIVATE_KEY: '0xac0974bec39a17e36ba4a6b4d238ff944bacb478cbed5efcae784d7bf4f2ff80'

      # Token address (deterministic from Anvil deployment)
      TOKEN_ADDRESS: '0x5FbDB2315678afecb367f032d93F642f64180aa3'

      # Amounts to send per request
      ETH_AMOUNT: '100'
      TOKEN_AMOUNT: '10000'

      # ── Solana (only active under the `solana` profile) ──
      # Treasury authority keypair mounted below; deterministic devnet USDC mint
      # from infra/solana/create-usdc-mint.sh. Drips SOL via airdrop + USDC from
      # the treasury. Defaults: 2 SOL + 1000 USDC per request.
      SOLANA_RPC_URL: http://solana-validator:8899
      SOLANA_USDC_MINT: H8HSreUF2s8r8hem4qMttE3bWYCpFuh71jbuos5bA77H
      SOLANA_FAUCET_KEYPAIR: /keys/usdc-authority.json

      # ── Mina (native-MINA treasury drip) ──
      # Signs a native payment client-side with mina-signer (no o1js proving) and
      # submits it to the PUBLIC Mina devnet via sendPayment. Reads/writes go to
      # MINA_GRAPHQL_URL. MINA_FAUCET_KEY is the treasury (HD index 2) base58
      # PRIVATE key — a SECRET, passed through from the host environment (never
      # hardcoded). When unset, the Mina route 503s with a public-faucet link, so
      # an EVM/Solana-only deploy still boots. The faucet asserts the derived
      # public key matches the treasury at startup (fail-loud on a wrong key).
      MINA_FAUCET_KEY: ${MINA_FAUCET_KEY:-}
      MINA_GRAPHQL_URL: ${MINA_GRAPHQL_URL:-https://api.minascan.io/node/devnet/v1/graphql}
      MINA_DRIP_AMOUNT: ${MINA_DRIP_AMOUNT:-5}

      # ── Mina USDC admin-mint (o1js zk-proving) ──
      # When MINA_USDC_ADMIN_KEY + MINA_USDC_TOKEN + MINA_USDC_ADMIN_CONTRACT are
      # all set, the /api/mina/request route ALSO admin-mints USDC (compiling the
      # UsdcChannelToken + FungibleTokenAdmin circuits once, ~6s, on the first
      # mint). MINA_USDC_ADMIN_KEY is the FUNDED mint-authority base58 PRIVATE key
      # (a SECRET — pass through from the host env, never hardcoded). When unset,
      # the route keeps dripping native MINA only. Reuses MINA_GRAPHQL_URL above.
      MINA_USDC_ADMIN_KEY: ${MINA_USDC_ADMIN_KEY:-}
      MINA_USDC_TOKEN: ${MINA_USDC_TOKEN:-}
      MINA_USDC_ADMIN_CONTRACT: ${MINA_USDC_ADMIN_CONTRACT:-}
      MINA_USDC_AMOUNT: ${MINA_USDC_AMOUNT:-1000}
      MINA_TX_FEE: ${MINA_TX_FEE:-}

    volumes:
      # Committed throwaway devnet treasury authority (mint authority + USDC
      # source). Read-only; only present/used under the solana profile.
      - ./infra/solana/usdc-authority.json:/keys/usdc-authority.json:ro
    ports:
      - '3500:3500'
    depends_on:
      # required:false so the faucet can also run under the `solana` profile
      # (where anvil isn't started) without compose erroring on the missing dep.
      # Under the evm profile anvil IS started, so the health gate still applies.
      anvil:
        condition: service_healthy
        required: false
    restart: unless-stopped
    logging: *default-logging
    healthcheck:
      test:
        ['CMD-SHELL', 'wget --no-verbose --tries=1 --spider http://localhost:3500/health || exit 1']
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s

  # ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  # Solana Test Validator - Local Solana with Auto-Deployed Programs
  # ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  solana-validator:
    image: ghcr.io/beeman/solana-test-validator:latest
    profiles:
      - solana
    entrypoint: []
    command:
      - sh
      - /entrypoint.sh
    volumes:
      - ./infra/solana/entrypoint.sh:/entrypoint.sh:ro
      # No committed fee-payer keypair: the entrypoint reuses the genesis-funded
      # validator-identity keypair the validator writes into its ledger dir as the
      # deploy fee-payer (the beeman image has no solana-keygen). The program id
      # stays deterministic via the mounted program keypair in /programs.
      # See infra/solana/entrypoint.sh.
      - ./packages/solana-program/target/deploy:/programs:ro
    # Validator ledger (genesis keypairs + state) lives in the container's
    # writable layer (DISK), not tmpfs. A RAM-backed tmpfs ledger fills under the
    # accounts/snapshot growth and the validator FREEZES after a few thousand
    # slots (observed on 2GB and 8GB alike); disk-backed keeps it producing. The
    # entrypoint's `--reset` still wipes it on every start, so chains stay
    # deterministic. `--limit-ledger-size` (entrypoint) caps growth.
    ports:
      - '8899:8899'
      - '8900:8900'
    security_opt:
      - seccomp=unconfined
    restart: unless-stopped
    # Bounded json-file logs (10m x 3 = 30 MB) so a chatty or crash-looping
    # validator can't fill the box disk via /var/lib/docker. The validator was
    # observed writing a 121 MB UNROTATED log; the prior 50m x 3 cap (~150 MB)
    # still allowed that, so this tightens it to a true 30 MB. The ledger itself
    # is bounded separately by --limit-ledger-size in entrypoint.sh. See the
    # x-logging anchor at the top of this file for the full incident note.
    logging: *default-logging
    # NB: /health returning "ok" only proves RPC is up — it does NOT prove the
    # validator is still producing blocks. A disk-full validator freezes its slot
    # yet stays "healthy" here; monitor getSlot advancing (and disk) out-of-band.
    healthcheck:
      test: ['CMD-SHELL', 'curl -sf http://localhost:8899/health | grep -q ok']
      interval: 10s
      timeout: 5s
      retries: 3
      start_period: 30s

  # ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  # Mina Lightnet - Local Mina Network with Accounts Manager & Archive
  # ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  # NOTE: Mina lightnet requires 4-8 GB RAM. Ensure Docker Desktop has
  # sufficient memory allocated (at least 8 GB recommended).
  mina-lightnet:
    image: o1labs/mina-local-network:compatible-latest-lightnet
    profiles:
      - mina
    ports:
      - '3085:3085' # GraphQL
      - '8181:8181' # Accounts manager
      - '8282:8282' # Explorer
      - '5433:5432' # Archive PostgreSQL (remapped to avoid conflicts with local Postgres on 5432)
    deploy:
      resources:
        limits:
          memory: 8g
    restart: unless-stopped
    logging: *default-logging
    healthcheck:
      test: ['CMD-SHELL', 'curl -sf http://localhost:8181/list-acquired-accounts || exit 1']
      interval: 15s
      timeout: 10s
      retries: 10
      start_period: 120s

  # ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  # Standalone Mode E2E — 2 connector containers + 2 app containers
  # ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  # Proves the connector Docker image boots with deploymentMode: standalone,
  # peers over BTP across the compose network, forwards packets via
  # /admin/ilp/send, and delivers to an app over HTTP /handle-packet.
  # Only the admin API ports are published to the host so the test harness
  # can drive assertions.
  standalone-app1:
    image: node:22-alpine
    profiles: [standalone-e2e]
    command: ['node', '/app/app.js']
    environment:
      PORT: '3100'
    volumes:
      - ./scripts/standalone-e2e/app.js:/app/app.js:ro
    ports:
      - '127.0.0.1:13101:3100'
    networks:
      - standalone_net
    healthcheck:
      test:
        ['CMD-SHELL', 'wget --no-verbose --tries=1 --spider http://127.0.0.1:3100/health || exit 1']
      interval: 5s
      timeout: 3s
      retries: 5
      start_period: 5s

  standalone-app2:
    image: node:22-alpine
    profiles: [standalone-e2e]
    command: ['node', '/app/app.js']
    environment:
      PORT: '3100'
    volumes:
      - ./scripts/standalone-e2e/app.js:/app/app.js:ro
    ports:
      - '127.0.0.1:13102:3100'
    networks:
      - standalone_net
    healthcheck:
      test:
        ['CMD-SHELL', 'wget --no-verbose --tries=1 --spider http://127.0.0.1:3100/health || exit 1']
      interval: 5s
      timeout: 3s
      retries: 5
      start_period: 5s

  standalone-peer1:
    build:
      context: .
      dockerfile: Dockerfile
    image: connector:standalone-e2e
    profiles: [standalone-e2e]
    environment:
      CONFIG_FILE: /app/config/peer1.yaml
      LOG_LEVEL: warn
    volumes:
      - ./scripts/standalone-e2e/peer1.yaml:/app/config/peer1.yaml:ro
    ports:
      - '127.0.0.1:18081:8081' # admin API
      - '127.0.0.1:18080:8080' # health
    depends_on:
      standalone-app1:
        condition: service_healthy
    networks:
      - standalone_net

  standalone-peer2:
    build:
      context: .
      dockerfile: Dockerfile
    image: connector:standalone-e2e
    profiles: [standalone-e2e]
    environment:
      CONFIG_FILE: /app/config/peer2.yaml
      LOG_LEVEL: warn
    volumes:
      - ./scripts/standalone-e2e/peer2.yaml:/app/config/peer2.yaml:ro
    ports:
      - '127.0.0.1:28081:8081' # admin API
      - '127.0.0.1:28080:8080' # health
    depends_on:
      standalone-app2:
        condition: service_healthy
    networks:
      - standalone_net

  # ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  # Tier-3 admin-API allowlist demo — app + connector in separate
  # containers on one compose network. Admin port is NOT published to
  # the host. The app reaches admin API via compose DNS; the connector
  # accepts the call because the app's source IP falls in the docker
  # bridge CIDR allowlist.
  # ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  standalone-allowlist-app:
    image: node:22-alpine
    profiles: [standalone-allowlist]
    command: ['node', '/app/app.js']
    environment:
      PORT: '3100'
      CONNECTOR_ADMIN_URL: http://standalone-allowlist-connector:8081
    volumes:
      - ./scripts/standalone-e2e/app.js:/app/app.js:ro
    ports:
      # Only the app is reachable from the host (test harness drives it).
      # The connector's admin port is intentionally NOT published.
      - '127.0.0.1:13401:3100'
    networks:
      - standalone_allowlist_net
    healthcheck:
      test:
        ['CMD-SHELL', 'wget --no-verbose --tries=1 --spider http://127.0.0.1:3100/health || exit 1']
      interval: 5s
      timeout: 3s
      retries: 5
      start_period: 5s

  standalone-allowlist-connector:
    build:
      context: .
      dockerfile: Dockerfile
    image: connector:standalone-e2e
    profiles: [standalone-allowlist]
    environment:
      CONFIG_FILE: /app/config/peer.yaml
      LOG_LEVEL: warn
    volumes:
      - ./scripts/standalone-e2e/peer-allowlist.yaml:/app/config/peer.yaml:ro
    # Admin API port (8081) is intentionally NOT in `ports:` — the allowlist
    # is a defense-in-depth layer on TOP of not publishing, not a replacement.
    depends_on:
      standalone-allowlist-app:
        condition: service_healthy
    networks:
      - standalone_allowlist_net

networks:
  standalone_net:
    driver: bridge
  standalone_allowlist_net:
    driver: bridge

